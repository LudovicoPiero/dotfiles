{colorscheme}:
with colorscheme.colors; {
  coolTheme = {
    ansi = [
      "#${base00}"
      "#${base08}"
      "#${base0B}"
      "#${base0A}"
      "#${base0D}"
      "#${base0E}"
      "#${base0C}"
      "#${base05}"
    ];
    brights = [
      "#${base03}"
      "#${base08}"
      "#${base0B}"
      "#${base0A}"
      "#${base0D}"
      "#${base0E}"
      "#${base0C}"
      "#${base07}"
    ];
    background = "#${base00}";
    cursor_bg = "#${base05}";
    cursor_fg = "#${base00}";
    cursor_border = "#${base05}";
    foreground = "#${base05}";
    selection_bg = "#${base05}";
    selection_fg = "#${base00}";
  };
}
